import { test, expect } from '@playwright/test';

test.describe('Amazon Fresh Grocessary Search', () => {
  test('Download all details for amazon fresh grocessary', async ({ page }) => {
    
  });
});
